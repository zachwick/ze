/**
 * @file templates.h
 * @brief External template file paths populated at runtime.
 * @defgroup templates Templates
 * @ingroup core
 * @{
 */
#pragma once

/** Path to the notes template (set in `main.c` via Scheme). */
extern char* notes_template;
/** Path to the README template (set in `main.c` via Scheme). */
extern char* readme_template;

/** @} */


